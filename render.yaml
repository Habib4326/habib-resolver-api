services:
  - type: web
    name: habib-media-resolver
    env: python
    buildCommand: |
      pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.0
